import * as Styled from '../../pages/Organization/styled';

const noRowsOverlay = () => (
  <Styled.NoRowsOverlay>
    <span>No Rows</span>
  </Styled.NoRowsOverlay>
);

export default noRowsOverlay;
