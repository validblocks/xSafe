import { Box } from '@mui/material';
import styled from 'styled-components';

export const ActionIconsBox = styled(Box)(({ theme: _ }) => ({
  width: '84px',
  marginBottom: 0.7,
  paddingLeft: '3px',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
}));
