import * as React from 'react';
import { Box } from '@mui/material';
import Divider from '@mui/material/Divider';

const DrawerAccounts = () => {
  return (
    <Box>
      <Divider />
    </Box>
  );
};

export default DrawerAccounts;
