name: multisig-deploy-staging

on:
  workflow_dispatch:
  push:
    branches:
      - 'develop'

jobs:
  deploy-staging:
    runs-on: [self-hosted]
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
    steps:
      - uses: act10ns/slack@v1
        with:
          status: starting
        if: always()

      - name: Checkout
        id: Checkout
        uses: actions/checkout@master
        with:
          ref: 'develop'
        if: success()

      - name: Create .env
        run: cat .env.staging > .env

      - name: Config setup
        run: |
          cp ./src/config.testnet.ts ./src/config.ts
          cat /dev/null src/multisigConfig.ts
          echo "export const uniqueContractName = '${{ secrets.UNIQUE_CONTRACT_NAME }}';" >> src/multisigConfig.ts
          echo "export const uniqueContractAddress = '${{ secrets.UNIQUE_CONTRACT_ADDRESS }}';" >> src/multisigConfig.ts

      - name: Deploy
        id: Deploy
        uses: up9cloud/action-rsync@v1.1
        if: success()
        env:
          HOST: docker.graffino.net
          USER: ec2-user
          KEY: ${{secrets.DEPLOY_SSH_KEY}}
          SSH_ARGS: ' -q -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no'

          VERBOSE: true
          ARGS: -carEzhvP --delete-before --exclude .git --exclude .github --exclude vendor --exclude .docker/logs --exclude storage --exclude node_modules --exclude README* --exclude public/docs --exclude bootstrap/cache

          TARGET: ~/docker/multisig.graffino.live
          SOURCE: ./

          PRE_SCRIPT: |
            echo "Start at: " $(date -u)
            mkdir -p ~/docker/multisig.graffino.live
            cd ~/docker/multisig.graffino.live
            sudo chown -R ec2-user:ec2-user ~/docker/multisig.graffino.live

          POST_SCRIPT: |
            cd ~/docker/multisig.graffino.live
            make staging
            echo "Stop at: " $(date -u)

      - uses: act10ns/slack@v1.2.2
        if: always()
        with:
          status: ${{ job.status }}
          steps: ${{ toJson(steps) }}
      - uses: act10ns/slack@v1.2.2
        with:
          status: ${{ job.status }}
          steps: ${{ toJson(steps) }}
